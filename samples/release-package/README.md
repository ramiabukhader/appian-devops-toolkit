# Sample Release Package

A minimal, fictional release package used to demonstrate
`Test-ReleasePackage.ps1`. It contains only a `manifest.json` and this README —
no real artifacts, secrets, or environment values.
